-- Phase 16 v3.1: fork-шаг становится «контейнером» — у него нет своего промпта,
-- юзер просто вводит название развилки и список веток. Промпты добавляются
-- как обычные prompt-шаги внутри ветки.
--
-- Схема: prompt_id у prompt-шага по-прежнему обязателен (валидация в Service);
-- у fork-шага — NULL. На уровне БД снимаем NOT NULL и оставляем валидацию в коде,
-- чтобы при апгрейде существующие fork-шаги (если такие есть в проде с промптом)
-- не сломались.

ALTER TABLE prompt_chain_steps
    ALTER COLUMN prompt_id DROP NOT NULL;
